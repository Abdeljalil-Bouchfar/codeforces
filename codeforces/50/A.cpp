#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

void solve(ll t)
{
    while (t--)
    {

    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll m, n;
    cin >> m >> n;
    cout << (m * n) / 2 << endl;
    return 0;
}